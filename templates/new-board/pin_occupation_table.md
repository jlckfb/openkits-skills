# [芯片型号] [板卡名] 引脚占用表

> 请根据原理图或引脚功能标注表填写实际引脚占用情况。
> 带 `？` 的项需要填写或确认，不存在的项可删除。

## 调试接口

| 引脚 | 功能 | 占用情况 | 备注 |
|------|------|----------|------|
| ？ | SWDIO | 不可使用 | 调试接口 |
| ？ | SWCLK | 不可使用 | 调试接口 |

## 系统级不可用引脚

| 引脚 | 功能 | 占用情况 | 备注 |
|------|------|----------|------|
| ？ | 频率精度控制 | 不可使用 | 系统占用，未引出 |
| ？ | HFXT 晶振输入 | 不可使用 | 系统时钟源 |
| ？ | HFXT 晶振输出 | 不可使用 | 系统时钟源 |

## 特殊注意事项引脚

| 引脚 | 条件 | 备注 |
|------|------|------|
| ？ | BSL 入口引脚 | 复位时需为低电平，否则进入 BSL 模式 |
| ？ | ？ | ？ |

## 板载外设占用

| 引脚 | 功能 | 外设实例 | 备注 |
|------|------|----------|------|
| ？ | 板载 LED | GPIO | 高/低电平亮？ |
| ？ | UART0 TX | UART0 | 板载串口 |
| ？ | UART0 RX | UART0 | 板载串口 |
| ？ | 用户按键 | GPIO | ？ |

## 引出到排针的全部引脚

> 列出所有引出排针的引脚（包括已占用的），用于判断可用性。

```
（按排针顺序列出所有引脚，如：PA0 PA1 PB2 ...）
```

## 汇总

| 类别 | 引脚列表 |
|------|----------|
| **完全不可用（系统占用）** | ？ |
| **有条件不可用** | ？ |
| **已占用（板载设备）** | ？ |
| **空闲可用** | ？ |

> 空闲可用 = 引出排针 - 不可用 - 有条件不可用 - 板载占用
